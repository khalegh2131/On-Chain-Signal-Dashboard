# ۰۴ — دروازه‌های کنترل کیفیت و نتیجه اجرا

هر مرحله پیش از بسته شدن، از فهرست کنترل زیر عبور کرده است. ستون «روش» دستوری است که هر کس می‌تواند تکرار کند؛ ستون «نتیجه» خروجی واقعی مشاهده‌شده است.

## مرحله ۲ — پایه پروژه

| # | مورد کنترل | روش | نتیجه |
|---|-------------|-----|--------|
| ۲.۱ | نصب وابستگی‌ها بدون تعارض | `npm install` | ۹۲۲ بسته، بدون ERESOLVE |
| ۲.۲ | بیلد تولیدی | `npm run build` | سبز، ۸ مسیر |
| ۲.۳ | ساختار پوشه‌ها مطابق معماری | بازبینی درختی | همه پوشه‌های لازم موجود |
| ۲.۴ | فایل نمونه متغیرهای محیطی | بازبینی `.env.example` | دقیقاً ۴ کلید خواسته‌شده |
| ۲.۵ | نبود کامنت‌های ماشینی | جست‌وجوی `TODO`/`coming soon`/`generated by AI` | صفر مورد |

## مرحله ۳ — لایه سرویس

| # | مورد کنترل | روش | نتیجه |
|---|-------------|-----|--------|
| ۳.۱ | صحت نوع‌ها | `npm run typecheck` | کد خروج ۰ |
| ۳.۲ | صحت لینت | `npm run lint` | بدون خطا و هشدار |
| ۳.۳ | آزمون سرویس‌ها | `npm run test` | ۶ فایل، ۶۰ تست، همه پاس |
| ۳.۴ | استفاده از نقاط پایانی واقعی | بازبینی مسیرها | فقط میزبان‌های واقعی؛ هیچ متد ساختگی |
| ۳.۵ | پوشش مسیر خطا | بازبینی تست‌ها | ۴۲۹، timeout، آدرس نامعتبر، شکست بالادستی |

## مرحله ۴ — هوک‌ها و سیستم طراحی

| # | مورد کنترل | روش | نتیجه |
|---|-------------|-----|--------|
| ۴.۱ | بیلد پس از افزودن کامپوننت‌ها | `npm run build` | سبز |
| ۴.۲ | آزمون تابع‌های خالص مشتق‌شده | `npm run test` | ۱۱ فایل، ۱۳۳ تست، همه پاس |
| ۴.۳ | عدم رگرسیون | مقایسه با مرحله ۳ | هیچ تستی از دست نرفت |
| ۴.۴ | کنترل هم‌رندر‌نشدن سرور/کلاینت | اجرای `next start` و بررسی HTML | بدون خطای hydration |

## مرحله ۵ — مونتاژ صفحات

| # | مورد کنترل | روش | نتیجه |
|---|-------------|-----|--------|
| ۵.۱ | وضعیت خالی هر صفحه | بازبینی + تست | برای هر چهار مسیر موجود |
| ۵.۲ | وضعیت بارگذاری | بازبینی | اسکلتون هم‌شکل با محتوای واقعی، بدون پرش چیدمان |
| ۵.۳ | وضعیت خطا با تلاش مجدد | تست تعاملی | دکمه تلاش مجدد تابع callback را فراخوانی می‌کند |
| ۵.۴ | پیام خطای فرم افزودن توکن | تست تعاملی | آدرس نامعتبر پیام می‌دهد و چیزی ذخیره نمی‌شود |
| ۵.۵ | فیلتر NFT | تست تعاملی | جست‌وجو، فیلتر زنجیره، مرتب‌سازی و حالت بی‌نتیجه |
| ۵.۶ | دسترس‌پذیری | بازبینی کد | برچسب ARIA روی دکمه‌های آیکونی، حلقه فوکوس، ناوبری کلیدی |

## مرحله ۶ — تأیید فنی نهایی (پس از پایان همه تغییرات)

| # | دروازه | فرمان | نتیجه واقعی |
|---|--------|-------|--------------|
| ۶.۱ | نوع‌ها | `npm run typecheck` | کد خروج ۰ |
| ۶.۲ | لینت | `npm run lint` | بدون خطا و هشدار |
| ۶.۳ | قالب | `npx prettier --check .` | همه فایل‌ها منطبق |
| ۶.۴ | آزمون | `npm run test` | ۲۰ فایل، ۲۹۷ تست، همه پاس |
| ۶.۵ | بیلد | `npm run build` | `BUILD_EXIT=0` |
| ۶.۶ | دود سرور | `next start` + درخواست هر مسیر | ۶ مسیر ۲۰۰ با محتوای درست، مسیر نامعتبر ۴۰۴، stderr سرور خالی |
| ۶.۷ | دروازه امنیتی SSRF | `npm run test` | ۴۵ تست حمله‌محور روی `lib/api/http.ts`، همه پاس |

## شرط عبور مرحله ۸ (بازبینی و تحویل)

| # | مورد | وضعیت |
|---|------|--------|
| ۸.۱ | بازبینی مستقل انجام شده | در سند ۰۹ ثبت می‌شود |
| ۸.۲ | ایرادهای یافته‌شده رفع یا ثبت شده | در سند ۰۹ ثبت می‌شود |
| ۸.۳ | تأیید نهایی کارفرما | **در انتظار** |

> قاعده بستن مرحله: هیچ مرحله‌ای با مورد کنترل ناقص بسته نشده است. تنها مورد باز در کل پروژه، تأیید نهایی کارفرما (۸.۳) است.