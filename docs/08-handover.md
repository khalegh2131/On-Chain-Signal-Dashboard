# ۰۸ — راهنمای راه‌اندازی، بهره‌برداری و انتقال

این سند طوری نوشته شده که یک توسعه‌دهنده بدون کمک سازنده بتواند پروژه را اجرا، استفاده و نگهداری کند.

## ۱. پیش‌نیازها

| مورد | نسخه پیشنهادی | تأییدشده روی |
|------|----------------|----------------|
| Node.js | ۱۸.۱۸ یا بالاتر | v22.22.0 |
| npm | ۹ یا بالاتر | 10.9.4 |
| سیستم عامل | هر جا Node اجرا شود | Windows 10.0.26100 |

## ۲. راه‌اندازی در پنج دقیقه

```powershell
# ۱. نصب وابستگی‌ها
npm install

# ۲. ساخت فایل محیطی از نمونه (اختیاری: بدون کلید هم کار می‌کند)
Copy-Item .env.example .env.local

# ۳. اجرا در حالت توسعه
npm run dev
# سپس http://localhost:3000
```

> نکته: پروژه بدون هیچ کلیدی هم بالا می‌آید و همه صفحه‌ها را با داده دمو نشان می‌دهد. کلیدها فقط برای خواندن داده زنده لازم‌اند.

## ۳. متغیرهای محیطی

| نام | الزامی؟ | کاربرد |
|-----|---------|---------|
| `NEXT_PUBLIC_ALCHEMY_API_KEY` | خیر (توصیه‌شده) | خواندن موجودی توکن، فراداده قرارداد و NFT. بدون آن، RPC عمومی جایگزین می‌شود که کندتر و محدودتر است. |
| `NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID` | خیر | اتصال کیف پول از طریق RainbowKit. بدون آن، اتصال کیف پول روی ارائه‌دهنده پیش‌فرض کار می‌کند اما تجربه محدودتری دارد. |
| `COINGECKO_API_KEY` | خیر | قیمت‌گذاری. بدون آن، سطح عمومی CoinGecko با محدودیت نرخ استفاده می‌شود. کلیدهای `CG-` خودکار به میزبان Pro هدایت می‌شوند. |
| `REDIS_URL` | خیر | رزرو برای کش توزیع‌شده در آینده. در نسخه فعلی کش درون‌حافظه‌ای است و این متغیر خوانده نمی‌شود. |
| `GRAPH_API_KEY` | خیر | خواندن موقعیت‌های Uniswap v3 از ساب‌گراف. |
| `AAVE_V3_SUBGRAPH_URL` | خیر | خواندن موقعیت‌های Aave v3. |

**قاعده امنیتی:** هرگز `.env.local` را در مخزن نسخه‌بندی نکنید. این فایل در `.gitignore` است. تنها `.env.example` (با مقادیر خالی) باید نسخه شود.

## ۴. دستورهای پروژه

| دستور | کار |
|--------|-----|
| `npm run dev` | اجرای سرور توسعه |
| `npm run build` | بیلد تولیدی |
| `npm run start` | اجرای بیلد تولیدی |
| `npm run lint` | بررسی ESLint |
| `npm run typecheck` | بررسی نوع‌ها بدون تولید خروجی |
| `npm run test` | اجرای آزمون‌ها یک‌باره |
| `npm run test:watch` | اجرای آزمون‌ها در حالت پایش |
| `npm run test:cov` | گزارش پوشش آزمون |
| `npm run format` | قالب‌بندی خودکار با Prettier |

## ۵. نقشه معماری برای نگهدارنده

```
app/          مسیرها و چیدمان (Server Components پیش‌فرض)
  (marketing)/    صفحه فرود
  (dashboard)/    داشبورد، پورتفوی، NFT، DeFi + loading/error هر مسیر
  api/            مسیر سلامت
components/   رابط کاربری
  ui/             پایه‌های shadcn
  shared/         چیدمان مشترک: نوار کناری، نوار بالا، فوتر، کارت سنجه
  portfolio/      جدول توکن، نمای کلی، دیالوگ واچ‌لیست
  charts/         نمودار و اسپارک‌لاین
  nfts/           گالری، کنترل‌ها و کارت NFT
  defi/           موقعیت‌ها، کارت و جزئیات
lib/          منطق
  api/            کلاینت‌های بیرونی + سلسله‌مراتب خطا + دروازه SSRF
  services/       منطق کسب‌وکار: موجودی، قیمت، کش، NFT، DeFi
  utils/          توابع خالص (قابل آزمون)
  mock/           داده دمو با بذر ثابت
  web3/           پیکربندی wagmi و هوک‌های کیف پول
hooks/        هوک‌های مشترک
config/       زنجیره‌ها و ثابت‌ها
types/        تایپ‌های دامنه مشترک
docs/         همین مستندات
```

**قاعده طلایی نگهداری:** منطق کسب‌وکار در `lib/services` می‌ماند، توابع خالص در `lib/utils`، و کامپوننت‌ها هیچ‌گاه مستقیم به یک ارائه‌دهنده بیرونی وصل نمی‌شوند. اضافه کردن یک ارائه‌دهنده جدید یعنی نوشتن یک کلاینت در `lib/api` و سرویس‌کردن آن، نه تغییر کامپوننت.

## ۶. کارهای متداول نگهداری

### افزودن یک زنجیره جدید
۱. یک ورودی در `CHAIN_METADATA` در `config/chains.ts` اضافه کنید (نام، اکسپلورر، زیردامنه Alchemy، شناسه CoinGecko، رنگ).
۲. اگر می‌خواهید به‌صورت پیش‌فرض خوانده شود، آن را به `ACTIVE_CHAINS` اضافه کنید.
هیچ تغییر دیگری لازم نیست؛ رابط کاربری، فیلترها و قیمت‌گذاری خودکار به‌روز می‌شوند.

### فعال کردن مِین‌نت
در `config/chains.ts` مقدار `ACTIVE_CHAINS` را به `[CHAIN_METADATA[1], CHAIN_METADATA[42161]]` تغییر دهید.

### افزودن پروتکل DeFi
یک تابع `readXPositions` در `lib/services/defi.service.ts` بنویسید که `PositionDraft` برگرداند و آن را به `Promise.all` در `fetchDefiPositions` اضافه کنید. ارزش‌گذاری خودکار انجام می‌شود.

### تنظیم TTL کش
مقدارهای `PRICE_TTL_MS` و مانند آن در `config/constants.ts` قرار دارند.

## ۷. عیب‌یابی

| نشانه | علت محتمل | کار |
|--------|-------------|-----|
| صفحه بالا می‌آید اما همه اعداد صفر است | کیف پول وصل نیست یا زنجیره پشتیبانی نمی‌شود | وصل کردن کیف پول؛ بررسی نشانگر زنجیره |
| ردیف توکن‌ها «بدون قیمت» است | توکن روی زنجیره‌ای است که CoinGecko نمایه نمی‌کند (شبکه آزمایشی) | رفتار درست است؛ برای قیمت، مِین‌نت را فعال کنید |
| صفحه DeFi فقط یادداشت پوشش نشان می‌دهد | `GRAPH_API_KEY` تنظیم نشده | افزودن کلید، بدون تغییر کد |
| قیمت‌ها دیر به‌روز می‌شوند | کش ۵ دقیقه‌ای قیمت | کاهش TTL در `config/constants.ts` |
| خطای ۴۲۹ از CoinGecko | محدودیت نرخ سطح عمومی | افزودن `COINGECKO_API_KEY` |
| بیلد با خطای ماژول‌های `@x402/*` | تغییر در زنجیره وابستگی RainbowKit | الگوی خنثی‌سازی در `next.config.mjs` را به‌روز کنید |

## ۸. انتقال به تیم بعدی — چک‌لیست

- [ ] `npm install` و `npm run build` را روی ماشین خود اجرا کنید و سبز بودن را ببینید.
- [ ] اسناد ۰۱ و ۰۲ را بخوانید تا دامنه و شاخص‌ها را بدانید.
- [ ] سند ۰۵ (ریسک) را بخوانید تا بدانید کجاها شکننده است.
- [ ] سند ۰۶ (تصمیم‌ها) را بخوانید تا دلیل انتخاب‌ها را بدانید و دوباره کاری نکنید.
- [ ] کلیدهای خود را در `.env.local` بگذارید (نه در مخزن).
- [ ] پیش از هر انتشار، سند ۰۲ را دوباره بسنجید.

## ۹. آنچه تحویل‌داده‌شده محسوب نمی‌شود

- انتشار روی محیط بیرونی (نیازمند تأیید جداگانه کارفرما).
- کلیدهای API واقعی.
- هر تعهد یا قرارداد به نام کارفرما.